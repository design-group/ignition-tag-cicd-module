pluginManagement {
    repositories {
        gradlePluginPortal()
		mavenCentral()
        maven {
            url 'https://nexus.inductiveautomation.com/repository/public'
        }
    }
}

rootProject.name = "tag-cicd"

include(
":",
    ":gateway"
)
